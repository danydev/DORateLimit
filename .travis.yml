osx_image: xcode8
language: objective-c
script:
- xctool test -workspace RateLimit.xcworkspace -scheme RateLimit\ Tests -sdk iphonesimulator ONLY_ACTIVE_ARCH=NO
before_script:
- brew update && brew reinstall --HEAD xctool || true
